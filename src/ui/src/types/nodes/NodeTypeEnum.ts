/**
 * Enumeration of node types supported in the agent builder
 */
export enum NodeTypeEnum {
    NONE = 'none' ,
    INPUT = 'input',
    OUTPUT = 'output',
    MODEL = 'model',
    STRING_FORMATTER = 'stringFormatter',
    CONDITIONAL = 'conditional'
}